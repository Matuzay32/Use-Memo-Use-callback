import React, {memo, useEffect, useState } from 'react';




const Item = memo(({user , handdleDelete}) => {
useEffect(() => {

    
    return () => {
    }
}, []);




    return (
        <li>
            {user.name}

            <button  onClick={()=>handdleDelete(user.id)}   >Delete</button>
        </li>
    );
});


export default Item;